%{# jinja-templates/Beamer-Harold.tex#}

i%--------------------- THEMES and FORMATTING -----------------------------------
\documentclass[
	17pt, % Set the default font size, options include: 8pt, 9pt, 10pt, 11pt, 12pt, 14pt, 17pt, 20pt
	%t, % Uncomment to vertically align all slide content to the top of the slide, rather than the default centered
]{article}


% Hyperref for long descriptions (according to documentation, import before geometry)
\usepackage{hyperref} % Will alow links to detailed explanations in appendix

\usepackage[margin=1in, nohead, nofoot]{geometry}

% Image Packages
\usepackage{graphicx} % For including images
\usepackage{caption} % For custom captions to those images
\usepackage{subcaption} % Some figures comprised of subfigures


% Set Graphics Folder Path:
\graphicspath{{../hjp-images-errors_and_snps_png/}}

% Custom Caption Style (no "Figure")
\captionsetup[figure]{labelformat=empty,singlelinecheck=false}

% Custom Section Heading (no numbering)
\setcounter{secnumdepth}{0}

\usepackage{amsmath} % Better math stuff
\usepackage{enumitem} % Make nested sublists prettier
%-------------------- Tikz for Creating Complex-er Images ----------------
% May not be able to add alt text!

\usepackage{tikz}
\usetikzlibrary{bayesnet}
\usetikzlibrary{arrows}
\usepackage{color}


%-------------------- Custom Commands for this Lecture: Counting Models ----------------

%\NewDocumentCommand{\imagewalt}{
%-------------------- Title, Auth, Date, etc Metadata -----------------------------------
\title{Counting Models}
\author{Harold Pimentel, PhD}
\date{}
%-------------------- BEGIN DOC BELOW  ----------------------------------------

\begin{document}

\frame{\titlepage}

{% for section in sections %}
\section{ {{ section.title }} }

{% for slide in section.slides %}
\begin{frame}
  \frametitle{ {{ slide.title }} }
  { {{ slide.content }} }
\end{frame}

{% endfor %}

{% endfor %}

\end{document}
